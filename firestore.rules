rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // COMPLETELY OPEN - NO RESTRICTIONS AT ALL
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 